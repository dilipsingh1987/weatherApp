import { StyleSheet } from 'react-native';

export const themeStyles = StyleSheet.create({
  lightText: {
    fontSize: 16,
    paddingVertical: 8,
    color: '#000',
  },
  darkText: {
    fontSize: 16,
    paddingVertical: 8,
    color: '#fff',
  },
});
